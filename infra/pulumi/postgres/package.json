{
  "name": "postgres-stack",
  "version": "0.1.0",
  "description": "Pulumi TypeScript stack for PostgreSQL deployment (RDS or local Docker)",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "up": "pulumi up -y",
    "destroy": "pulumi destroy -y",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/docker": "^4.0.0",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/random": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
